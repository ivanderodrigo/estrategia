name: Investigación estratégica profunda semanal

on:
  workflow_dispatch:
  schedule:
    # Domingo: investigación exhaustiva y recalibración antes de la semana.
    - cron: "47 3 * * 0"

permissions:
  contents: write

concurrency:
  group: westcon-public-intelligence
  cancel-in-progress: false

jobs:
  research:
    runs-on: ubuntu-latest
    timeout-minutes: 150
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - name: Python 3.12
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"
      - name: Dependencias
        run: pip install requests
      - name: Preflight offline del motor
        run: |
          python -m py_compile scripts/research.py scripts/validate.py scripts/selftest.py
          python scripts/selftest.py
      - name: Investigación profunda
        env:
          RESEARCH_PROFILE: deep
          BRAVE_SEARCH_API_KEY: ${{ secrets.BRAVE_SEARCH_API_KEY }}
          BASE_API_TOKEN: ${{ secrets.BASE_API_TOKEN }}
        run: python scripts/research.py --profile=deep
      - name: Validar dataset antes de publicar
        run: python scripts/validate.py
      - name: Guardar actualización
        run: |
          git config user.name "westcon-strategy-bot"
          git config user.email "actions@users.noreply.github.com"
          git add data/research.latest.json data/research_status.json data/changes.latest.json data/history/
          git diff --cached --quiet || git commit -m "chore: investigación estratégica profunda"
          git pull --rebase origin main
          git push origin HEAD:main
