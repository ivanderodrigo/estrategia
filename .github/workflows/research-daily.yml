name: Westcon Intelligence Daily

on:
  schedule:
    - cron: "17 5 * * *"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  research:
    uses: ./.github/workflows/research-run.yml
    with:
      profile: daily
      max_runtime: 720
      fallback_runtime: 0
      timeout_minutes: 35
      commit_message: "chore: inteligencia pública diaria"
