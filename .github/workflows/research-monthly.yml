name: Westcon Intelligence Monthly

on:
  schedule:
    - cron: "41 3 1 * *"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  research:
    uses: ./.github/workflows/research-run.yml
    with:
      profile: exhaustive
      max_runtime: 3300
      fallback_runtime: 300
      timeout_minutes: 70
      commit_message: "chore: inteligencia exhaustiva mensual"
